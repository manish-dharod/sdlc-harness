#!/usr/bin/env bash
# Sanctioned supervisor -> Claude Code implementation capsule runner.

set -u

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export AGENT_CAPSULE_WORKER_TOOL=claude-code
exec "$ROOT/scripts/agent-capsule-run" "$@"
